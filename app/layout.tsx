import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Providers } from "@/components/providers/ProvidersComponent";
import { ThemeProvider } from "@/components/ui/theme-provider";
import { StorageProvider } from "@/components/providers/StorageContext";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Mini Drive - Personal Cloud Storage",
  description: "5GB free personal cloud storage with Google authentication",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <ThemeProvider attribute="class" defaultTheme="system" enableSystem>
          <StorageProvider>
            <Providers>{children}</Providers>
          </StorageProvider>
        </ThemeProvider>
      </body>
    </html>
  );
}
